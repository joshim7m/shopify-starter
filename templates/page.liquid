<section class="mt-3 py-16">
  <div class="container">
    <div class="my-7">
      <h1 class="font-medium text-2xl">{{ page.title }}</h1>
    </div>

    <div>
      <p>{{ page.content }}</p>
    </div>
  </div>
</section>